# Postgres connection string (Neon, Supabase, or local Docker)
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=

# Public site URL (used in share strings, canonical links)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
